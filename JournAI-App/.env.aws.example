# AWS RDS Configuration Example
# Copy this file to backend/.env when deploying to AWS

# Application
PORT=3000
NODE_ENV=production

# AWS RDS PostgreSQL
DB_CLIENT=postgres
DB_HOST=journai-prod.xxxxxxxxxx.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=journai
DB_USER=journai
DB_PASSWORD=your-secure-rds-password-here

# Security
JWT_SECRET=your-jwt-secret-minimum-32-characters-long

# OpenAI (Optional)
OPENAI_API_KEY=sk-proj-your-openai-key-here

# AWS Specific (Optional)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# S3 for uploads (Future enhancement)
# S3_BUCKET=journai-uploads
# S3_REGION=us-east-1

# RDS Proxy (Recommended for production)
# Use RDS Proxy endpoint instead of direct RDS endpoint
# DB_HOST=journai-proxy.proxy-xxxxxxxxxx.us-east-1.rds.amazonaws.com
